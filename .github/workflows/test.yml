name: Build
on: [push]
jobs:
  build-and-test-dbstats:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [24] # both the HA add-on (ghcr.io/home-assistant/base:3.24, apk nodejs-npm)
        # and the standalone Docker image (node:24-alpine) now run Node 24
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
      - name: Install shared deps
        working-directory: ./shared
        run: npm ci
      - name: Install backend deps
        working-directory: ./backend
        run: npm ci
      - name: Install frontend deps
        working-directory: ./frontend
        run: npm ci
      - name: build front
        working-directory: ./frontend
        run: npm run build
      - name: build back
        working-directory: ./backend
        run: npm run build
      - name: test backend
        working-directory: ./backend
        run: npm run test
      - name: lint backend
        working-directory: ./backend
        run: npm run lint
      - name: lint frontend
        working-directory: ./frontend
        run: npm run lint
